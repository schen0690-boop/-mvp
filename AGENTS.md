# 项目协作规则

## 范围与阶段

项目为 AI 圆桌讨论 Web App MVP，实际使用 Codex 辅助开发。需求来源只取题面前三部分及用户明确指令。当前阶段4D：P8授权DeepSeek官方适配器、配置/提示词/本地TDD和一次受限联调。先完成本地验证，再交接密钥配置，必须等用户回复“已配置”才进行真实调用。真实请求累计最多2次、1场指定话题4专家；持久化保护不得重置。不实现调度/SSE/演播厅。

阶段4D-B执行状态（2026-09-16）：用户已明确确认旧密钥撤销、新密钥未公开；指定唯一真实样本已通过，实际1次请求，确认及刷新完成。原调用授权已因成功关闭，剩余1次不能另用；不得删除预算或重启以获得新生成机会。用户私有配置不得查看/修改/归档。后续阶段需新的明确授权。

- 每轮只完成该轮明确授权的阶段，不自行安装、提交、联网调用模型或进入实现。
- 阶段1B明确授权在 `tools/env-probe/` 局部安装精确依赖、编写探针与持久化环境测试、启动loopback服务及独立测试浏览器。探针不是产品功能/样例数据，不整体迁入正式应用；不把环境测试结果改写为产品TDD/E2E通过。
- 修改前读取 `docs/requirements.md` 和对应的架构、契约、UI、测试设计；涉及公共字段时同时检查相关文档。
- 来源 A＝题面明确要求，B＝用户明确确认，C＝设计建议，D＝未确认事项。不得把 C、D 写成既定事实。
- 当前根目录为 `D:\实测文件夹`。路径变更先查文件与仓库边界，不覆盖、不搬移未知内容。
- 保持中文沟通；独立判断需求，直接指出冲突并给出最小解决方案。

## 技术基线

React + Vite + TypeScript 前端；Node.js + Express + TypeScript 后端；SQLite；HTTP 操作请求 + SSE 公开事件；Vitest 核心测试；项目内 `@playwright/test` 和持久化测试文件；npm 与后续锁文件。

`node:sqlite` 已通过阶段1B探针，本轮由用户明确选定用于草稿模块，并补充真实业务集成测试。业务层只依赖本项目明确的数据访问操作，不能直接依赖驱动 API。不使用 Codex 私有缓存包作为项目安装或交付条件。不擅自扩展账户、音视频、分布式系统或通用事件平台。

## 实现和验证边界

- 只有获准实现后才编写业务代码。核心逻辑必须先写能体现行为的测试，实际看到目标原因的失败，再最小实现、验证通过；真实保存结果，不用事后补测试冒充 TDD。
- 类型共用不替代运行时校验。HTTP 参数、SSE 游标、数据库边界、模型结构化输出都按契约验证；字段白名单与关联关系也须校验。
- 模型输出是不可信数据，不能执行其中的指令、HTML、脚本或工具操作。没有运行时校验的输出不能写为公开事实。
- 用户输入不得成为系统指令。模型适配器不具备任意工具执行能力，不接受浏览器指定的模型服务地址。
- 密钥只由后端环境变量读取；不在聊天、日志、Git、样例、前端环境变量或截图中写真实密钥。不枚举敏感环境变量，不读取现有密钥原值。
- 只向前端发布公开发言、真实运行状态、独立生成且校验的公开关注点、共识/分歧和可见错误。隐藏推理、意愿评分、内部调度事件、原始模型响应和诊断栈不得进入公开 DTO/SSE。
- 模型等待期间不持有数据库写事务。按讨论隔离上下文、任务、数据、事件及取消信号；持久化前核对讨论、运行代次和源 transcript 版本。
- 第一版参数为用户确认的可配置默认值而非题面数字：4专家另加主持人、专家1–8、running/stopping合计最多2场；每场/全局模型并发2/4，所有适用调用纳入；30秒完整响应超时，重试仅由协调服务一层管理、总计最多2次尝试。12次成功持久化的专家发言或进入running后10分钟触发收尾；收尾总期限60秒含排队/调用/重试，最终总结暂为1–2句。模型适配器与SDK不得再自动重试放大。
- 正常系统流程的测试替身只放在外部模型边界；阶段3另明确允许独立标记的网络延迟、断线、异常响应故障注入，不以其冒充正常真实服务结果；自动化 E2E 运行真实前端、后端、SQLite 与事件链路。真实模型检查和人工质量审查单独记录。

## 过程证据

- 不虚构测试结果、历史 Prompt、工具使用、开发模型身份或 Git 记录；计划、已执行和已验证分开。
- 实际开发工具为 Codex；开发用具体模型未核实，不猜测。阶段4D用户确认官方DeepSeek/deepseek-flash/关闭思考/非流式JSON/4096；真实能力与价格未验证。
- SDD 仅沿用“数据建模与 API 契约”，DDD 仅沿用“前端组件与页面”；不补写英文全称。
- 仅能取得真实原文的开发 Prompt 才原样归档；应用专家角色 Prompt 不计入核心开发 Prompt。
- 用户暂定本仓库Git身份为schen <cs064210@163.com>，仅设置当前仓库；阶段3已授权先建立截至阶段2基线，再按实际小任务提交，不倒填时间、不重写历史、不创建远程仓库或推送。
- 阶段4B使用writing-plans及当前会话逐任务执行，核心TDD、异常systematic-debugging、完成前verification-before-completion；4A设计已获P6确认，历史报告保留原语境。用户明确不启用额外代理/worktree，继续当前仓库并按真实小任务提交。
- 本轮不启动 worktree、并行子代理、分支清理。使用现有 Superpowers，实际工具接口及用户边界优先于 Skill 默认流程。
- 每轮结束报告文件变化、实际验证、已知问题、待确认项及下一阶段最小计划，并等待授权。
