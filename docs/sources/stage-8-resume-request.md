我已在本机完成GitHub浏览器授权，登录命令正常结束。
请继续阶段8，沿用此前已经批准的发布范围。

本地项目：
D:\实测文件夹

唯一目标：
[https://github.com/schen0690-boop/-mvp.git](https://github.com/schen0690-boop/-mvp.git)

此前状态：

- 远程初始化README已通过921f337合入；
- 本地开发历史保留；
- 当前报告中的本地HEAD：
  3aa420cbbacf0c6435a649721d66891a175dbab1
- 推送此前因缺少认证退出128；
- 远程仍需重新核对，不假定它没有变化。

一、先做必要核对

确认：

- 当前分支为main，工作区干净；
- HEAD与上述候选的关系；
- origin的fetch和全部push地址都只指向指定仓库；
- 认证能够用于本次Git操作。

不要读取、打印或归档凭据。
若HEAD已有额外变化，先分类说明，不盲目覆盖。

执行fetch并检查最新origin/main是否为本地main的祖先。
此前合并已经完成，不重复执行无关历史合并。

若远程新增了本地尚未包含的提交：
暂停并报告差异，不自动pull、rebase或强制覆盖。

二、普通推送main

条件满足后，执行：

git -c push.followTags=false push -u origin main:main

不使用：
\--force、--force-with-lease、--mirror、--all、--tags，
也不删除.git、重建仓库或改写历史。

若仍有认证或权限错误：
保留准确的脱敏错误与退出码，停止对应操作。
不要反复修改全局凭据配置或要求用户提供Token。

推送结果不确定时先查询远程提交，
不能仅依据命令失败就认定远程未更新。

三、完成此前尚未执行的远程核验

核对：

1. 远程main完整哈希与预期发布提交一致；
2. 仓库页面显示完整项目及正式README；
3. 原本地开发历史和远程初始化提交均保留；
4. 未登录访问可以读取公开仓库；
5. README、API/SSE、样例、Prompt索引、
   工作流与交付报告的关键链接有效；
6. 7张Mermaid图在GitHub页面的实际渲染情况。

从远程完整克隆到新的独立目录，
核对HEAD、Git文件树及历史。
不使用浅克隆，不覆盖原项目。

若应用文件树与已验收版本一致，
本轮不重复安装依赖或运行全部产品测试。
发现实际缺失或变化时，再补对应检查。

无法查看某张图的渲染时，明确标为未验证，
不能以源码可读取代替渲染通过。

四、更新记录并收尾

只有推送与远程核验真正完成后，
才把“远程仓库及评阅访问”标为完成。

必要的发布记录可以形成一个文档提交并正常推送；
随后核对最终本地和远程HEAD。
不要反复制造“更新自身提交哈希”的提交。

继续保留：
“出题方对Codex工具口径的确认”这一外部待办。
不得自行标记已获许可。

本轮不改应用代码、不启动应用、
不调用真实模型、不修改旧授权或验收库，
不部署网站、不改变仓库可见性。

最终报告：

- 推送是否成功及实际退出码；
- 最终本地/远程完整提交哈希；
- 仓库访问、完整克隆和历史核验结果；
- 关键链接及7张Mermaid远程显示结果；
- 本轮提交、工作区状态；
- 尚未完成的事项。

完成后停止。
