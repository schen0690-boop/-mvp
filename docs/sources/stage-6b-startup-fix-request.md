请处理：

【6B 启动脚本修复与零外呼彩排】

项目根目录：
D:\实测文件夹

依据本次受阻报告：
临时启动脚本将原生 Fetch 的 response.ok 当作函数，
误判就绪并触发退出；真实讨论未开始，官方请求为0。
此前6B授权已经关闭，原记录必须保持关闭。

本轮只修复并验证启动流程。
不启动真实讨论，不创建或激活新的真实调用授权。

一、先核实根因

读取：

- docs/stage-6b-validation.md；
- 本次实际使用的启动脚本及其执行记录；
- 健康检查、异常捕获和退出清理逻辑；
- 当前package.json、相关测试和Git状态。

定位：

- 实际失败的文件与行；
- response对象究竟来自原生fetch还是Playwright；
- 原始异常如何被处理成“服务未就绪”；
- 哪条退出路径关闭了授权。

先用本地响应复现，再修改。
若报告中的定位与实际代码不一致，报告差异，不猜测修复。

不要全局替换.ok()。
原生Fetch使用response.ok；
Playwright响应按其真实API使用ok()。

二、最小修复

只修改启动检查、必要的辅助函数和测试。
不更换模型，不调整runner、调用预算、期限或提示词。

健康检查应区分：

- 服务尚未启动或临时连接失败；
- HTTP非成功响应；
- 返回内容不符合现有健康接口契约；
- 脚本自身的类型错误或编程错误。

可恢复的启动等待使用明确的总超时和有限重试。
脚本编程错误应报告准确原因，
不能被吞掉后笼统写成“服务启动超时”。

不以固定sleep代替就绪检查，
不以“端口能连接”或“HTTP有响应”直接认定应用就绪。

沿用真实健康接口的响应约定，
不要凭空增加健康字段或暴露密钥、内部路径。

三、让实际脚本纳入验证

如果原脚本只存在于临时命令中，
将必要启动逻辑保存为可测试的项目脚本，
保持现有启动方式，不建立新的通用启动框架。

按现有工程方式：

- TypeScript脚本纳入类型检查；
- JavaScript脚本补必要类型说明或checkJs检查；
- 不通过any、忽略错误或通用鸭子类型绕过问题。

本地测试至少覆盖：

1. 实际Fetch Response返回正常健康结果；
2. 服务先不可达、随后就绪；
3. 404/503不能误判成功；
4. 内容不符合健康接口契约；
5. 请求超时能有限退出；
6. 编程错误与服务未就绪被正确区分；
7. 失败清理幂等，只关闭本轮自有资源；
8. 成功就绪后不误走失败退出路径。

测试必须调用将用于验收的实际检查函数，
不能在测试中重新写一份正确逻辑代替它。

四、同入口零外呼彩排

使用修复后的同一套启动与清理逻辑，运行：
正式前后端
＋独立测试数据库
＋本地预置阵容
＋Fake或本地HTTP stub
＋独立测试授权夹具。

本地彩排至少验证：
启动服务
→ 后端健康检查
→ 前端代理连接
→ 浏览器读取已确认阵容
→ 本地短讨论
→ 结束和刷新
→ 资源清理。

这只是本地彩排，不能计作真实6B通过。

要求：

- 明确拒绝非本地模型请求；
- 不加载真实密钥；
- 不修改官方地址安全限制；
- 不使用此前6B真实数据库或预算记录；
- 不通过测试夹具重新打开旧授权；
- 不启用整条流程自动重试来掩盖错误。

测试授权夹具应明确标记为测试数据，
不得被真实入口误用。

保持真实入口和本地彩排的启动逻辑一致，
仅替换被允许的配置和外部依赖。
不要在正式重试时再临时编写未经验证的新脚本。

五、旧授权与证据保持不变

此前真实6B：

- 授权保持closed；
- 原discussion/run、预算记录和受阻结果保留；
- 不改写为成功；
- 不因为请求为0而恢复原额度；
- 不修改4D授权或验收库。

新真实验收仍需用户另外批准。
本轮不创建可实际外呼的新授权。

六、验证、记录与提交

执行：

- 启动脚本类型/静态检查；
- 本轮回归测试；
- 同入口零外呼彩排；
- 若修改共享启动模块，运行对应受影响回归。

不机械重跑与改动无关的全部产品测试，
但必须说明本轮实际复验范围。
旧测试成绩不能写成本轮重新通过。

在原验证报告中追加“启动脚本修复”小节，
保留原受阻结论；保存实际RED→GREEN与彩排证据。
不为这次局部修复另建一套阶段设计文档。

沿用当前项目级Git身份合理提交，
不改写历史、不推送，不提交密钥、数据库或预算原始文件。

结束时释放本轮自有端口、浏览器和测试锁，
不删除未知占用或用户数据。

最终报告：

1. 实际根因、失败位置和修复范围；
2. 实际启动脚本如何纳入类型检查与测试；
3. 测试和同入口彩排的命令、结果与退出码；
4. 官方请求为0的验证方式；
5. 旧授权及原受阻记录保持不变；
6. Git提交与资源清理；
7. 是否具备申请新一次真实验收的本地条件。

完成后停止，不进行真实调用。
