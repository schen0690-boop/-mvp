请在已完成阶段7及补充收口的基础上，执行：

【阶段8：发布到现有GitHub仓库与远程交付核验】

本地项目：
D:\实测文件夹

唯一目标仓库：
[https://github.com/schen0690-boop/-mvp.git](https://github.com/schen0690-boop/-mvp.git)

仓库网页：
[https://github.com/schen0690-boop/-mvp](https://github.com/schen0690-boop/-mvp)

本轮允许：

- 核对并配置该仓库的远程连接；
- 获取远程历史；
- 按下述限定合并远程初始化README提交；
- 正常推送本地main及其可达历史；
- 从远程重新克隆并核验交付文件和历史；
- 更新必要的发布记录。

本轮不新建仓库、不改名、不改变当前公开可见性，
不新增功能、不调用真实模型、不重新开启旧授权。

不得使用强制推送、重写历史或重新初始化项目，
不得只上传最终文件而丢弃真实开发过程。

一、本地发布前检查

进入真实项目根目录，读取：

- AGENTS.md；
- docs/delivery-validation.md；
- 当前README；
- Git状态、分支、远程和最近提交。

核对：

- 当前分支是否为main；
- 工作区是否干净；
- 阶段7补充收口提交0bfafea是否存在；
- 当前HEAD与已验收候选之间是否有额外修改；
- 应用代码与已测试提交ddf1dc2的对应关系。

上述提交编号是核对依据，不代替实际检查。
若发现未说明的应用代码、依赖或配置变化，
先报告并补必要验证，不直接称其为已验收版本。

记录发布前本地HEAD与文件树标识。
不清理用户数据，不删除测试库、私有配置或旧预算。

本轮主要改变Git连接、合并元数据及必要文档。
应用代码未变时，不机械重跑全部产品测试。

二、敏感信息与发布范围

复用阶段7已有检查方式，
核对main将推送的文件及其可达历史，
重点检查开发Prompt、证据、配置、日志和截图。

不得发布：

- 真实API Key、Token或Authorization；
- .env.backend.local等私有配置；
- 用户业务库或验收数据库；
- 原始授权预算文件；
- 隐藏推理或未脱敏供应商响应；
- node_modules及不必要的大型临时产物。

不含真实密钥的配置示例和脱敏验收证据可以保留。
不要因为文件名含env就误删合格的example文件。

若已有覆盖相同提交范围的检查证据，可以复用；
新增文件和新增历史范围需要补查。

只报告疑似敏感内容的位置、类型和处置建议，
不得在检查输出中重新打印秘密。

若发现真实凭据曾进入待发布历史：
暂停推送，报告问题。
不绕过GitHub保护，不擅自清洗或改写历史，
也不因凭据已失效就自动公开它。

如存在推送触发的Git hooks或Actions，
先确认不会意外调用真实模型或外部付费服务。
本轮不新增Actions、GitHub Secrets、Pages或Release。

三、认证与远程配置

使用现有Git凭据管理器或已登录的GitHub CLI。
不读取、打印或归档凭据。

若缺少认证，让用户本人在本地完成官方登录流程，
完成后继续；不索取密码、Token或SSH私钥。

检查origin：

- 不存在时，添加为指定仓库；
- 已经指向指定仓库时，直接沿用；
- 指向其他仓库时，暂停并说明，不静默覆盖。

同时核对fetch URL与实际push URL，
保证本轮只有一个目标，不向其他地址推送。
URL中不得嵌入Token。

允许在确认origin不存在时执行：
git remote add origin [https://github.com/schen0690-boop/-mvp.git](https://github.com/schen0690-boop/-mvp.git)

先读取远程分支及HEAD，再fetch。
认证失败、网络失败不能当成“远程为空”。

四、处理远程现有README提交

网页检查时，远程main只有一个README，
内容为“# -mvp”。
执行时必须重新获取并检查，不能假定状态没有变化。

检查远程提交、文件树和与本地main的祖先关系。

按实际情况处理：

A. 远程尚无分支
直接准备正常首次推送，不制造合并提交。

B. 远程main已经是本地main的祖先
无需额外合并，准备正常快进推送。

C. 远程仍只有README占位内容，且与本地无共同祖先
本轮允许一次受控合并，保留两侧历史。

可采用：
git merge --allow-unrelated-histories --no-commit --no-ff origin/main

若仅README冲突：

- 保留合并前已经验收的本地完整版README；
- 将冲突明确解决并检查；
- 保留远程初始化提交作为合并历史的一部分；
- 创建真实的合并提交，不倒填时间。

不要用全局“全部选ours”的方式解决冲突，
不要把本地README替换成只有仓库标题的远程版本。

提交前比较合并结果与原本地候选：
若只是接入远程初始化历史，应用文件树应保持不变。
发现非预期文件变化时停止检查，不直接提交。

D. 远程已有其他实际内容、更多未审阅提交，
或出现README之外的冲突
暂停并报告具体差异。
不得用覆盖、自动rebase或强制推送处理。

禁止：

- git push --force或--force-with-lease；
- 使用“+”refspec强制更新；
- reset --hard到远程；
- 删除.git后重新初始化；
- squash本地开发历史；
- 将项目复制进新仓库再只提交一次；
- 改写作者、提交时间或历史Prompt。

五、正常推送

发布前再次核对：

- 合并冲突全部解决；
- 当前main包含原本地全部开发历史；
- 远程初始化提交按约定保留；
- 暂存区和工作区状态明确；
- 目标push URL正确；
- 敏感信息检查无未解决问题。

只推送main，不批量推送其他分支、标签或备份引用。
可使用：

git -c push.followTags=false push -u origin main:main

不使用--mirror、--all或--tags。

若遇到：

- non-fast-forward；
- 认证或权限失败；
- 分支保护；
- 秘密扫描拦截；
- 大文件限制；

读取实际错误并暂停对应操作。
不关闭保护，不绕过检查，不自动强推或重写历史。

推送命令失败但结果不确定时，
先查询远程main的实际提交，
不要盲目重新合并或改写本地。

六、远程交付核验

不能只凭push退出0就宣布交付完成。

至少验证：

1. 远程refs/heads/main指向本次预期发布提交。
2. 仓库页面显示完整项目，而不是只有占位README。
3. 正式README、src、web、tests、e2e、scripts、
   样例和docs等实际交付内容可读取。
4. Git历史保留本地开发演进，
   原本地候选仍为最终发布提交的祖先。
5. 仓库在未登录状态下可读取，符合当前公开设置。
6. 关键相对链接、Prompt索引、工作流和验证报告可访问。
7. 当前交付文档中的7张Mermaid图在GitHub实际显示情况。

图表必须查看渲染结果；
不能因为Markdown源码可下载就写“远程渲染通过”。
无法核验时准确标为未验证。

再从该远程地址完整克隆到一个新的本地目录：

- 不使用原项目目录；
- 不使用浅克隆来验证完整历史；
- 不复制原目录的.git、依赖或私有配置；
- 使用明确的新目录名，避免覆盖已有文件。

核对远程克隆的HEAD、Git文件树和历史，
与预期发布结果一致。
文件比较优先依据Git对象，
不要把工作区LF/CRLF转换误判为应用内容变化。

阶段7已完成干净安装和产品测试；
若远程文件树与已验收应用一致，
本轮不必再次重复全量安装和产品测试。
若发现缺失或变化，明确报告并补受影响检查。

七、文档与剩余外部事项

必要时在README或现有交付报告中加入仓库入口，
只改当前维护文档，不重写历史Prompt中的本机路径。

只有推送和访问核验实际完成后，
才能将“远程仓库及评阅访问”标为完成。

“出题方对Codex工具口径的确认”仍单独保留：
没有真实确认信息，不得标为已解决，
不得将实际开发工具改写成Claude Code。

本轮只发布源码仓库，
不部署网站，不新增账户系统或修改产品能力。

追加本轮真实开发Prompt及发布过程记录。
若发布核验后的文档更新形成新提交，
只正常推送该提交，并核对最终远程HEAD。

不要为了让报告写入它自身的最终提交哈希，
反复制造无意义的“更新哈希”提交。
最终发布哈希可以在最终回复中报告。

八、最终回复与停止

报告：

1. 是否成功发布，以及实际仓库网页；
2. 远程可见性和main分支状态；
3. 最终本地/远程完整提交哈希；
4. 远程README初始化历史的处理方式；
5. 原本地开发历史是否保留；
6. 远程克隆与文件树核验结果；
7. README、关键链接及Mermaid远程显示结果；
8. 本轮实际修改、执行检查和未验证项目；
9. 本地工作区、临时资源与Git状态；
10. 剩余的Codex工具口径外部待办。

没有成功完成push和远程核验，
不得写“代码已上传”或“交付全部完成”。

本轮官方模型请求必须为0。
旧4D、6B及R1授权和记录保持不变。

完成后停止，不自动部署或继续开发。
