请在阶段7现有成果上完成：

【阶段7补充收口：锁文件核对、版本一致性与文档渲染】

项目根目录：
D:\实测文件夹

本轮不新增产品功能，不重新审计整个项目，
不调用真实模型，不创建远程仓库、不推送。

目标是补齐三个具体事项：

1. 核对干净目录安装是否确实复用了提交中的锁文件；
2. 明确最终候选版本与已测试提交的差异；
3. 实际渲染当前交付文档中的Mermaid图并检查链接。

一、读取实际证据

读取：

- AGENTS.md；
- docs/delivery-validation.md；
- evidence/stage-7/clean/verification.json及有关安装日志；
- 当前依赖清单和锁文件；
- 当前Git状态及ddf1dc2之后的提交；
- README与当前正式交付文档。

不要依据报告措辞直接判定代码有错误。
先检查实际命令、文件和差异，再决定是否需要修改。

保留历史Prompt、原始验收记录、数据库和已关闭的授权。
不修改私有配置，不读取或输出真实密钥。

二、核对锁文件安装

确认阶段7实际执行的安装命令，
以及干净目录使用的依赖清单和锁文件来源。

检查所有参与复现的正式包目录，
不能只核对根目录而遗漏独立前端包。

若实际使用提交中的锁文件执行npm ci，
且锁文件前后未变：

- 将“重新锁定安装”等歧义表述改为准确措辞；
- 记录实际命令与对应证据；
- 不重新生成锁文件，不为措辞问题重跑全部测试。

若锁文件被重新生成、修改，或缺乏足够证据：

- 明确实际情况；
- 查明是文档缺失、安装策略还是依赖不一致；
- 不静默替换锁文件；
- 仅在有必要时，在新的干净目录按候选提交补做验证。

不得使用--force、升级依赖或删除锁文件掩盖问题。
安装脚本沿用项目既有安全策略。

三、确认被测版本与最终候选版本

核对：

- 被测提交ddf1dc2；
- 后续提交10b7d28；
- 当前HEAD。

列出源码、依赖、配置、样例、文档和证据的变化分类。

若后续只有文档和证据：
明确记录应用代码与被测提交的对应关系，
不重复执行与变更无关的全量测试。

若存在影响运行、构建、初始化或测试行为的变化：
执行对应复验；
必要时从新的候选提交重新完成相关干净目录验证。

不要把所有Markdown变更都自动视为无影响：
README启动命令、配置说明或安装步骤变化，
仍需检查它们是否与已经执行的流程一致。

报告分别记录：

- 实际被测源码提交；
- 本轮文档/证据提交；
- 两者差异及验证范围。

不为追求报告包含它自身的最终提交哈希，
反复制造“更新哈希”的提交循环。

四、Mermaid实际渲染

范围只包括当前正式交付文档中的有效图表：
架构、ER、状态机、调用/事件流程等。

不要改写历史Prompt、历史方案和原始失败记录中的图表。

先枚举：
文件、标题或位置、图类型、渲染结果。

优先使用已有本地工具。
缺少工具时，允许在隔离验证目录获取官方固定版本的
Mermaid渲染依赖，先检查必要安装脚本。

限制：

- 不修改应用依赖及其锁文件；
- 不安装全局工具；
- 不为了渲染额外配置Docker/WSL；
- 不上传项目文档到外部在线编辑器；
- 优先复用已有浏览器；
- 无法完成时准确记录阻塞，不伪称通过。

实际渲染并查看：

- 语法是否有效；
- 中文是否完整；
- 标签是否被截断；
- 箭头、状态和关系是否可读；
- 图中的字段/状态是否与现有文档一致。

只修复语法、排版或明确的文档漂移，
不借机修改业务设计。

保留Markdown中的Mermaid源代码，
不要只用图片替换源码。
必要时保留少量SVG/PNG作为渲染证据，
避免重复生成大批无用附件。

记录渲染器版本和所用命令。
本地通过不能写成GitHub/Gitee远程显示已通过。

五、交付文档与链接

核对README到以下材料的实际相对链接：

- API/SSE契约；
- 架构和数据文档；
- 五组样例及导入说明；
- 开发Prompt索引；
- 工作流说明；
- 总交付报告。

从实际Git跟踪文件确认路径，
不要仅凭聊天中的转义形式推断文件不存在。

当前维护文档中的路径修正为仓库可用的相对链接。
历史Prompt原文保持不变。

检查图表补充后，需求与交付检查表的状态是否准确。

六、边界与外部待办

保留以下外部待办：

- GitHub/Gitee实际仓库链接及评阅访问；
- 出题方对Codex开发工具口径的确认。

本轮不猜测平台、账号、仓库名或可见性，
不创建仓库、不添加远程、不推送。

未取得出题方确认时，
不能把Codex使用情况改写成Claude Code，
也不能把模型配置等同于工具使用证明。

真实非空观点语义、长期质量、跨平台、
移动真机、全面安全/无障碍等，
仍按实际覆盖情况列为限制。
不为补齐这些标签追加真实模型调用或扩大本轮任务。

七、记录、提交与停止

优先更新现有delivery-validation.md，
不新增一套重复的阶段报告。

记录本轮真实Prompt、修改原因和执行证据。
只提交实际文档修正、必要的小脚本及精选渲染证据。

沿用现有项目级Git身份，
不改写历史、不推送，不提交私有配置、数据库或密钥。

最终报告：

1. 实际安装方式及锁文件是否保持不变；
2. 被测提交与最终候选代码的关系；
3. Mermaid图总数、渲染结果及实际检查范围；
4. 文档链接核对结果；
5. 本轮修改与必要复验；
6. 剩余本地问题和两项外部待办；
7. Git状态、提交及临时资源清理。

没有必要的代码变更时，不重跑全部产品测试。
完成后停止，不继续扩功能、不自动发布。
