请基于已经完成的阶段 6B-R1，进入并实际执行：

【阶段 7：交付核对、必要修补与干净目录复现】

项目根目录：
D:\实测文件夹

目标：
对照原题前三部分，检查项目是否具备完整、真实、
可运行、可复查的交付材料，并修补明确的小范围缺口。

本轮不追加真实模型调用，所有既有真实授权保持关闭。
不得为补截图、改善内容或增加样例重新请求模型。

不重新设计架构，不继续扩展讨论功能。
先检查已有产物；确实缺失或错误时才补充。

一、范围与现有依据

读取：
- 原题前三部分的可用原文及需求追踪；
- AGENTS.md、README；
- 当前架构、API/SSE契约、测试计划；
- 开发Prompt和工作流记录；
- 当前样例、初始化、迁移和启动脚本；
- 阶段5C、6A、6B及R1验证记录；
- 正式源码、测试配置、Git状态与历史。

第四部分“提交方式”继续排除。
第三部分要求的源码仓库、提交历史等仍需核对，
不能因排除第四部分而漏掉第三部分交付物。

不要依据文件名或此前摘要认定内容完整。
原文无法取得的要求标记来源缺失，不自行补造。

继续使用现有调试、TDD和完成前验证能力。
本轮可以进行明确的安全和界面审查：

- openai/skills 中的 security-best-practices；
- vercel-labs/agent-skills 中的 web-design-guidelines。

已存在则沿用；未存在时允许读取官方仓库中的技能、
必要参考文件及许可证，固定来源版本。
不重复安装Superpowers，不执行未知安装脚本，
不因技能不可用而停止其他交付核对。

二、建立唯一交付检查表

优先更新现有需求追踪，不另建重复的需求体系。
每项记录：
原始要求、实际实现/文档位置、验证证据、状态、剩余问题。

区分：
- 本地自动化已验证；
- 真实单样本已验证；
- 人工检查；
- 未验证；
- 缺失或等待用户/出题方确认。

不要用一个总“通过”覆盖不同证据等级。

至少核对：

产品：
首页、动态阵容与确认、主持人职责、自主发言机制、
专家公开状态、Transcript、实时共识/分歧、自然语言总结。

技术：
前后端分离、SQLite、后端密钥、多讨论隔离、
实时更新、中文响应式、分区独立滚动。

交付：
源码、初始化/迁移、至少五组样例、
Markdown开发文档与Mermaid图、API文档、
测试、README、真实Git演进、核心开发Prompt、
1–1.5页工作流说明。

保持题面工具口径问题：
若尚无证据证明出题方接受Codex，
如实列为待外部确认，不虚构已经获准使用Claude Code。
应用使用deepseek-flash与开发工具使用何种模型分开说明。

三、样例数据：先查再补

核对至少五组完整的“话题＋对应主持人和专家阵容”。

每组需：
- 话题明确，专业视角有差异；
- 恰好一位主持人及对应expertCount位专家；
- 姓名、职业、Title、立场完整；
- 系统ID、颜色、顺序符合现有规则；
- 明确虚构角色与预置样例来源。

不足时允许在本地编写和审阅样例，不调用模型。
不要为了凑数把同一组成员换话题重复五次。

样例应有可复现的导入方式，
通过既有校验和必要事务保存。
重复导入不重复堆数据、不覆盖用户已有讨论、
不自动确认新用户阵容、不启动讨论或模型调用。

在界面或README中说明如何找到样例。
不要让五组样例只存在于测试内部、用户无法查看。

这些样例不等于五次真实模型成功记录。
测试中预置的发言、观点也不能冒充真实讨论输出。

四、运行入口与README

核对并明确区分：

A. 无密钥的Fake演示/自动化测试；
B. 用户显式配置后使用的真实Provider模式；
C. 4D、6B、R1等一次性历史验收入口。

正常使用不能依赖某个已经关闭的验收授权、
固定discussion ID或你本机的私有数据库。

检查普通用户是否能根据文档显式选择已有Provider。
若只能通过历史验收脚本启动，列为交付缺口。
允许复用现有配置和启动逻辑补最小接线，
不得重置历史授权或自动开启真实调用。

真实模式的启动/配置能力只用本地替身验证。
默认保持安全的Fake模式；
真实模式失败不静默切回Fake；
页面应能准确说明当前演示模式。

README至少包含：
- 实测运行环境与未验证环境；
- 从项目根目录安装、初始化、导入样例、
  启动前后端、运行各层测试的准确命令；
- 后端环境变量、密钥本地填写与忽略规则；
- Fake和真实模式区别，以及真实调用可能产生费用；
- 技术选型与主要HTTP/SSE接口；
- 已完成功能、已知限制和后续改进；
- 端口冲突、数据库锁、模型配置错误的安全处理；
- 验证报告和核心文档入口。

不得把Windows本机验证写成跨平台认证，
不得把移动视口测试写成移动真机验证。

当前可维护文档中的本机D盘绝对链接，
能改为仓库相对链接的应修改。
历史Prompt原文不因路径整理而重写。

五、开发文档与过程材料

检查架构、状态图、ER图、API和SSE文档
是否与最终代码一致，而不是停留在草稿阶段。

核对所有仍有效的Mermaid图：
能使用现有工具渲染则实际检查；
不能渲染时如实标记，不伪称已经验证。

核心开发Prompt：
- 至少五段真实原始输入；
- 覆盖题面指定的SDD、DDD、TDD、E2E任务；
- 每段附1–2句意图、实际挑战或修正说明；
- 明确文档位置和阶段对应关系；
- 不拿应用运行时角色提示词代替；
- 不补造没有发生过的Prompt或失败测试。

可以从已有真实归档整理精选索引，
不必把全部长对话重复复制进一个新文件。

工作流说明：
用Markdown形成题目要求的约1–1.5页说明，
写明实际使用Codex的流程、Skills的实际作用、
2–3个有记录的典型问题与解决路径，
以及对工程化AI开发的理解。

选用真实问题，不为凑数量编造事故。
Markdown分页取决于渲染，不只凭字数声称精确页数；
说明采用的预览或版式口径，不额外制作无必要的多格式文件。

六、真实证据的表述核对

保持原6B受阻、启动修复和R1成功记录彼此区分。
不得覆盖失败记录，也不把多次验收拼成一次连续成功。

R1明确写：
- 阵容预置；
- 真实短讨论；
- 两次专家发言；
- 一次合法空提炼；
- 九次真实请求；
- 本次未验证非空观点的真实证据质量。

核对“小范围试点”表述：
读取实际公开发言和总结，
区分它是对原话的概括、主持人新增建议，
还是没有依据的归因。

记录具体支持或缺口，不自行补外部来源，
不修改历史模型输出使其看似正确。

先检查已有非空共识/分歧测试是否充足；
缺失时可以补本地人工构造的发言与观点夹具，
验证引用关联、覆盖版本、显示和更新。
明确这不是新的真实模型语义验证。

不能因证据ID存在就宣称语义支持一定正确。
若需要改提示词或业务行为，先列为独立问题；
本轮不因一个短样本自动重调全部模型提示词。

七、安全与界面专项审查

安全检查范围：
- 私有配置、当前跟踪文件、Git历史和可发布证据；
- 密钥、鉴权头、原始响应、隐藏推理是否泄漏；
- 前端产物是否混入后端配置；
- 模型输出/话题是否被当作HTML执行；
- 查询、事件、角色及证据引用是否按讨论隔离；
- 普通客户端是否能控制Provider地址、预算或故障模式；
- SSE连接清理、有限缓冲和错误脱敏；
- 真模型是否可能被普通测试或启动脚本意外触发。

扫描输出只报告位置、类型和处理建议，
不要再次输出疑似密钥值。
不读取用户私有配置正文来完成审查。

若发现真实凭据进入提交历史：
暂停发布准备，报告脱敏位置，
申请受控清理，不擅自重写历史或复制秘密。

不以“扫描无命中”宣称完成全面安全保证。
安全Skill是审查依据，不等于自动漏洞证明。

界面检查：
关键操作、中文状态、错误反馈、键盘焦点、
1/4/8位专家、长文本、独立滚动与窄屏可访问性。

只修复明确影响要求、可用性或安全的局部问题。
不重做视觉风格，不新增账户、音视频或云部署。

新代码修复必须先复现并增加相关测试。
涉及数据丢失风险、状态语义变化或大规模重构时，
列为阻塞项并停止相关修改，不自行扩大范围。

八、从干净目录复现

完成必要修补并形成候选提交后，
在项目外的新临时目录中，
使用该提交的已跟踪文件进行复现。

不要直接复制整个工作目录，
也不要复制：
node_modules、私有配置、已有数据库、
旧预算记录、缓存或本机生成的构建产物。

按真实包结构，在所有必要包目录执行锁文件安装。
使用npm ci及项目既有的安装脚本安全策略，
不通过修改锁文件、使用--force或升级依赖掩盖失败。

允许下载锁定依赖，不修改全局环境。
安装失败时区分网络问题、版本问题与文档缺失。

完全按README执行：
依赖安装
→ 类型检查和构建
→ 新库初始化/迁移
→ 五组样例导入
→ Fake前后端启动
→ 核心用户流程
→ 相关自动化测试
→ 服务停止和重开读取。

确认没有从原目录或Codex私有缓存隐式导入项目依赖，
测试不会因继承真实密钥而外呼。

复现中发现遗漏步骤，应补入README，
不能只在临时目录手工修好后声称可复现。

最后一次代码、样例或配置修改后，
运行最终验收所依赖的后端、前端、Fake E2E、
HTTP替身及启动测试、类型检查和构建。

记录实际被测源码提交、目录、版本、命令、数量和退出码。
若最后只修改验证报告，说明代码未变，不机械重复全部测试。

同一台电脑的新目录复现，不等于跨平台测试通过。

九、Git、发布待办与收尾

保留真实提交演进，不squash、rebase或倒填时间。
合理提交本轮实际修补、样例和文档。

检查是否已经存在真实的GitHub/Gitee仓库链接。
若没有，列为第三部分交付待办；
本轮不擅自创建远程仓库、推送或改变可见性。

源码快照可用于本地复现，
不能把不含Git历史的压缩包当作完整仓库历史。

不签署或虚构出题方工具许可，
不猜测远程账号、仓库名或访问权限。

保留历史验收库和已关闭预算；
本轮只清理自己创建的临时服务和安全可删测试产物。
不要删除未知锁、用户数据或私有配置。

最终交付：
1. 需求与交付物检查表；
2. 五组样例及导入验证；
3. 与实际代码一致的README、API和开发文档；
4. 精选开发Prompt索引与工作流说明；
5. 安全/界面审查结果及已修复项；
6. 干净目录复现和最终回归证据；
7. 尚需用户处理的真实阻塞项。

优先只新增一份总报告：
docs/delivery-validation.md
其他内容更新已有文档，不复制出多套规范。

最终回复：
- 本地交付准备完成，还是仍有明确阻塞；
- 各交付物的实际位置与状态；
- 本轮修改和最终测试结果；
- 非空观点、真实质量、跨平台等未验证项；
- 远程仓库与工具口径等外部待办；
- Git提交、工作区和清理结果。

本轮官方模型请求必须为0。
完成后停止，不自动发布或追加真实讨论。