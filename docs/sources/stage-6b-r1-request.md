用户已经明确批准以下新授权，请执行：

【6B-R1：修复启动后的独立单样本真实讨论验收】

项目根目录：
D:\实测文件夹

本轮是一次新的独立验收，不是重新开启旧6B授权。
复用已经验证的启动逻辑，执行一次真实短讨论并收尾。
不重新开发适配器、不扩大产品功能、不再重复申请相同授权。

一、授权范围

固定使用：

- DeepSeek官方直连；
- deepseek-flash；
- thinking关闭；
- 非流式JSON响应；
- 不启用工具调用；
- 四种操作的输出token上限：
  意愿512、公开发言768、提炼4096、总结1024。

样本：

- 话题：“AI 如何改善教育？”
- 1位预置虚构主持人＋2位预置虚构专家；
- 计划2次成功提交的专家发言；
- 第一条专家发言后进行1次中途提炼；
- 最后执行唯一一次逻辑总结任务。

限制：

- 普通阶段最长120秒；
- 收尾最长60秒；
- 单次完整模型调用最多30秒，且服从剩余阶段期限；
- 总模型请求最多20次；
- 普通任务最多18次；
- 总结最多2次尝试，包含在20次总额内。

所有网络重试、输出修复和额外主持介入均计入总额。
第一次成功就使用结果，不为用完次数而重试。

正常无重试路径预计9次请求，仅供核对，
不是必须达到的调用数，也不是成功的唯一判据。

禁止：

- 调用模型重新生成阵容；
- Hello、额外连通性、模型列表或余额请求；
- 更换模型、放宽校验或提高token上限；
- 第二场讨论、自动补跑、重置预算；
- 复用旧6B或4D的剩余额度。

二、先核对现有修复与入口

读取：

- AGENTS.md；
- README中的6B运行方式；
- docs/stage-6b-validation.md及启动修复记录；
- scripts/startup/readiness.ts；
- scripts/startup/launch.ts；
- scripts/stage6b-dry-run.mjs；
- 当前受限真实入口、授权保护及短参数配置；
- Git状态。

核对提交513f797及后续相关修改，
确认本次实际入口确实使用已验证的启动与清理逻辑。

不要重新编写临时健康检查命令，
不要另写一份未验证的启动脚本替代launch.ts。

若需要为R1增加入口参数或薄包装器：
只复用现有逻辑，修改后先执行启动类型检查及定向测试。
不改变runner、模型适配器、重试或业务状态语义。

完成不联网的安全配置检查：

- 本地密钥已配置；
- 实际模型与Provider符合授权；
- 私有配置被忽略且未被Git跟踪；
- 不输出密钥、文件正文、Authorization或完整环境变量。

真实密钥只能由后端运行时加载。
不得从聊天记录、其他工具账户或全局凭据中获取密钥。

若前置检查存在安全缺口，暂停并报告。
检查通过后直接继续，不再次询问是否同意20次额度。

三、建立独立R1环境与授权

使用新的：

- 6B-R1验收数据库；
- 授权与预算记录；
- discussion ID；
- run ID；
- 证据目录。

路径遵循项目现有规范，
与旧6B及4D路径明确分开，并在执行记录中写清楚。

复用此前已经审阅的预置阵容夹具，
通过既有校验和导入方式保存，并正常确认。
不直接复制旧运行数据库或修改状态绕过校验。

阵容来源标记为：
“本地预置，未在本次通过真实模型生成”。

授权只绑定本次唯一discussion/run，
仅允许四种讨论操作，不允许真实阵容生成或第二个run。

保护必须继续保证：

- 发送前原子检查、持久化计数；
- 并行请求不突破20/18/2限制；
- 普通任务不能占用总结预留；
- 已发送的失败、超时和未知结果不退还次数；
- 重启不清零；
- 关闭后不能重新启用。

已有同名R1记录时先读取状态：
不得覆盖、删除或另换路径规避已有计数。
若已执行或已关闭，报告状态，不重新授权。

旧6B、4D数据库和授权记录保持不变。
尤其不得重新开启旧6B的closed记录。

四、启动与就绪检查

使用已验证的launch/readiness逻辑启动：

- 受限真实后端；
- 指向该后端的前端；
- 独立Edge测试上下文。

只监听本机回环地址。
端口按现有配置和实际空闲情况确定并记录。
不复用来源不明的服务，不清理未知锁或占用。

启动就绪检查只能访问本地健康接口和必要快照，
不能通过调用模型判断服务是否就绪。

确认：

- 前端实际连接R1后端；
- 讨论Provider为真实DeepSeek，不是Fake/stub；
- 页面显示本次预置阵容；
- 阵容处于lineup\_confirmed；
- 短参数和调用保护实际生效；
- 没有其他真实模型任务运行。

只有这些检查通过，才开始真实讨论。

若仍在开始前失败，按现有安全退出策略处理并保留证据。
即使官方请求仍为0，也不得自行关闭后再重开授权。

五、执行一次真实讨论

通过现有页面：

读取已确认阵容
→ 点击一次“开始讨论”
→ 主持人开场
→ 专家基于当前内容申请并发言
→ 第一条专家发言后中途提炼
→ 第二次专家发言
→ 收尾与总结
→ 刷新读取同一结果。

不先直接请求模型，再通过页面重复执行。

不要启用会重跑整条真实流程的自动测试重试。
真实运行期间不修改源码、提示词或参数。

两次专家发言不要求两位专家各一次。
不能为了覆盖角色临时改成固定轮流。

无人申请、无效输出或候选失败，
按既有有限处理执行，不为凑齐发言扩大次数。

中途提炼允许共识或分歧为空，
不得补造观点来让界面显得完整。

正常路径达到2次专家发言后进入收尾，
不继续派发额外专家发言或第二次中途提炼。
总结仍依据最后已提交的完整内容。

六、断线、失败与结果未知

浏览器断线、截图失败、开始/结束响应丢失：
先GET同一discussion并核对run和计数。
不得再次创建、开始或重新生成。

允许重新订阅SSE、刷新和只读查询；
这些操作不能产生额外模型调用。

只保留runner已有的有限重试。
外层脚本和浏览器不得另加模型重试。

普通预算不足或阶段到期：
停止新的普通任务，按既有规则进入有限收尾，
不得使用总结预留继续普通讨论。

总结失败：
如实保留summary.unavailable，
不在终态后额外补调总结。

致命故障或进程中断：
停止新的调用，保留数据与授权计数。
不重启runner续跑，不重新开一场。

成功、最终失败或验收中止后关闭本次授权。
未使用次数不再用于调参、截图补测或另一场验收。
关闭过程不能提前误杀尚在合法收尾期限内的总结任务。

若需要修复本轮发现的问题：
在授权关闭后使用本地替身复现和修复。
修复后再次真实运行需要新的用户授权。

七、工程链路与内容质量分别验收

工程链路检查：

- 唯一discussion/run与短配置正确；
- 调用经过真实适配器及既有校验；
- 公开发言和观点由正式事务保存；
- SSE实际推送中途变化；
- 总结与终态正确；
- 刷新后内容、版本、状态一致；
- 停服后安全只读重开能读取相同结果。

阶段成功不能仅依据completed字段。
未完成计划的两次发言、中途提炼或有效总结，
必须明确记录覆盖不足或降级结果。

内容审阅只看本次真实公开结果：

- 角色关注点是否与话题相关；
- 后续发言是否回应当前上下文；
- 是否出现明显重复或互不相关的独白；
- 提炼证据是否存在且支持对应观点；
- 是否把单人观点错误包装成全体共识；
- 总结是否忠实保留实际内容与分歧。

发现语义问题如实记录，
不删改模型结果掩盖，不追加请求追求更好答案。

本次只验证一个短讨论样本，
不宣称长期质量、所有话题、全部人数或生产负载通过。

八、证据与用量

在现有stage-6b验证报告中新增“6B-R1”独立小节，
或按现有规范建立对应记录，
保留原6B受阻结论和启动修复历史。

记录：

- 实际源码提交与执行时间；
- 新数据库、端口及授权记录位置；
- 预置阵容来源；
- 唯一discussion/run；
- 实际发言、提炼和总结次数；
- 每次请求的operation、attempt、状态、耗时；
- 请求/响应模型名及finish\_reason；
- 实际取得的usage；
- 最终业务状态与授权状态。

区分：
预算预留、发送前计数、有证据的实际发送、成功响应。
响应未知或usage缺失明确标注，不当成0。

已知usage合计不冒充完整账单，不估造费用。
不要用此前9次stub请求或4D阵容用量代替本次真实数据。

保存经过筛选的公开轨迹与代表截图：

- 运行中；
- 中途提炼；
- 总结/终态；
- 刷新后的相同结果。

错过中途截图时使用已有事件证据并说明，
不能重跑讨论或制作伪造截图。

禁止归档：
密钥、Authorization、完整环境变量、
reasoning\_content和未脱敏原始供应商响应。

九、回归、Git与清理

本轮原则上是验收，不扩大代码修改范围。
若只更新文档与证据，无需再次机械重跑全部产品测试。
若修改了入口或共享代码，运行对应定向测试并记录。

过去通过的测试写为历史结果；
本轮真正执行的检查另列命令和退出码。

结束时：

- 关闭R1授权并保留原始计数；
- 取消未派发任务，释放本轮连接和服务；
- 关闭独立浏览器上下文；
- 核对自有端口和测试占用；
- 不清理未知进程、旧数据库或历史预算；
- 不声称本地取消能够证明供应商绝无后续计费。

使用当前已确认的项目级Git身份，
提交必要入口变更、脱敏证据及文档。
不提交私有配置、验收库或敏感原始记录，
不改写历史、不推送远程。

最终报告：

1. R1结果：正常路径通过/降级结束/失败/受阻；
2. 实际入口、源码版本和验收环境；
3. 唯一discussion/run与预置阵容；
4. 实际发言、提炼、总结次数及终态；
5. 各类模型请求数、总数、耗时和可得usage；
6. 20次总限额及总结预留的执行情况；
7. 保存—SSE—展示—总结—刷新检查结果；
8. 内容审阅的具体发现和未覆盖项；
9. 证据路径、Git提交及资源清理；
10. 新授权关闭，旧6B和4D记录保持不变。

完成后停止。
不自动执行第二场真实讨论，不进入下一开发阶段。
