我把/D:/实测文件夹/.env.backend.local的密钥进行了更换，接下来进行



【阶段 4D-B：一次受限真实阵容联调与验收】

项目根目录：
D:\实测文件夹

当前状态以最后一次交接为准：

- DeepSeek 适配器、本地测试和持久化调用预算保护已完成；
- .env.backend.local 已由用户本人配置；
- 本地配置检查退出码为 0；
- 私有配置已忽略、未被 Git 跟踪；
- 此前官方模型请求为 0 次；
- 真实联调尚未执行。

本轮不是重新开发 4D-A。
只做必要前置核对、启动现有受限入口、
执行一个真实样本、保存证据并关闭调用授权。

一、密钥安全前置条件

用户此前曾在聊天中暴露过一把完整 API Key。

真实调用前，需要用户确认：
“此前密钥已撤销，当前本地配置使用未公开的新密钥。”

若已有该确认，直接继续；
若尚未确认，只询问这一项并暂停真实调用。
不得索取、查看、输出或比对新旧密钥原值。

本地只读核查可以继续。
不要将“密钥已配置”误认为“泄露密钥已经更换”。

不得：

- 打印 .env.backend.local 正文；
- 输出 Authorization 或完整环境变量；
- 将密钥写入命令行、日志、截图或开发 Prompt；
- 覆盖用户私有配置；
- 调用旧密钥来测试它是否仍然有效。

二、调用授权与固定参数

本轮激活的是此前同一份授权，不是新增两次额度。

固定设置：

- 官方地址：[https://api.deepseek.com](https://api.deepseek.com)
- 模型：deepseek-flash
- thinking：disabled
- stream：false
- response\_format：json\_object
- max\_tokens：4096
- 话题：“AI 如何改善教育？”
- expertCount：4，另加 1 位主持人

范围：

- 仅一次逻辑阵容生成；
- 绑定唯一 discussion 和 generation；
- 累计最多两次模型请求，包含重试及输出修复；
- 首次成功就不发送第二次；
- 首次确定性错误也不为用完额度而继续请求。

禁止：

- Hello 或额外连通性模型请求；
- 比较模型、换话题、重新生成第二套阵容；
- 额外调用模型列表、余额等非必要官方 API；
- 手动清零预算、更换计数文件或换数据库绕过额度；
- 更换模型、启用思考、增加 token 上限或放宽校验。

既有单次 30 秒、generation 总期限 60 秒保持不变。
浏览器等待失败不能成为扩大模型调用期限或次数的理由。

三、必要前置核对

读取与本轮执行直接相关的：

- AGENTS.md；
- README 中真实联调运行方式；
- docs/stage-4d-validation.md；
- package.json 中真实联调脚本；
- src/live-server.ts；
- 持久化预算保护和安全指标输出逻辑。

不要重新审计整个项目，也不要修改已经通过的适配器。

执行现有的安全检查：

- npm run config:check；
- 确认私有配置被忽略且未跟踪；
- 核对 Git 状态；
- 核对实际验收数据库、预算文件和服务端口；
- 确认本轮受限入口实际使用 DeepSeek，而不是 Fake。

检查只输出非敏感设置和“密钥已配置/未配置”。
不要通过读取私有文件正文进行人工验证。

读取现有预算记录：

- 新授权记录不存在时，只按现有机制初始化一次；
- 已有记录时保留原计数与绑定；
- 若记录已有请求或已关闭，不擅自重置；
- 若记录与“此前 0 次请求”不一致，先报告差异；
- 已有成功结果只能继续查看或确认，不另行生成。

启动前确认预算保护没有缺口。
若无法保证两次上限，停止真实调用。

四、启动现有受限环境

使用 README 已定义的 npm run live:stage4d，
以及它实际需要的前端启动方式。

按照现有配置使用独立数据库及 41851/41852 端口；
具体前后端对应关系从代码和 README 确认，不猜测。

要求：

- 只监听本机回环地址；
- 前端 /api 确实连接受限真实后端；
- 不误连普通 npm start 的 Fake 服务；
- 不连接、迁移或清空未经确认的用户业务库；
- 不复用来源不明的服务；
- 端口被占用时不误杀其他进程。

可以执行不会调用模型的本地健康检查。
不得将模型请求作为启动就绪探针。

使用项目已有浏览器能力和独立测试上下文。
不访问日常浏览器资料，不下载新浏览器，不增加依赖。

五、执行唯一一条真实流程

通过现有中文页面完成：

1. 创建指定话题、4 位专家的草稿；
2. 记录服务端返回的 discussion 标识；
3. 点击一次“生成阵容”；
4. 确认受限入口绑定本次 generation；
5. 等待现有服务完成生成；
6. 成功后查看阵容；
7. 确认当前版本；
8. 刷新页面并重新读取已确认阵容。

禁止先直接请求模型一次，再用页面生成一次。

浏览器自动化不得设置会重跑整条流程的重试。
不要因按钮未及时出现、截图失败或浏览器断线，
重新执行“创建—生成”。

若页面操作中断：
先 GET 同一 discussion，核对服务器状态；
保留同一数据库、预算记录和 generation；
只恢复读取、展示或确认，不重新生成。

生成阶段的第二次模型请求，
只能由现有服务在同一 generation 和剩余预算内执行。
不得由浏览器或外层脚本另外重试生成操作。

生成成功后的查询、确认、刷新和截图，
均应复用同一阵容，不触发任何额外模型请求。

六、成功、失败和结果未知的处理

A. 生成成功

核对：

- 恰好 1 位主持人、4 位专家；
- 姓名、职业、Title、立场完整；
- 角色 ID、颜色和顺序由系统控制；
- 经既有校验与正式事务保存；
- 页面展示与公开快照一致；
- 确认和刷新后状态、成员及版本一致；
- 不展示隐藏推理、内部错误或原始 JSON。

人工阅读一次公开阵容，
简要检查角色是否与话题相关、关注点是否存在差异。
不要因为姓名或措辞不够理想再次生成。

B. 生成最终失败

保留安全错误码、请求次数和终态证据。
停止本次真实生成，关闭剩余授权。

不得：

- 点击“重试生成”开启新 generation；
- 切换 Fake 冒充成功；
- 修改数据库状态或手工插入阵容；
- 放宽解析、人数或字段规则；
- 修复后擅自再启动一次真实生成。

后续可以使用本地 HTTP stub/Fake 复现和修复，
但新的真实生成需要另行授权。

C. 浏览器或 HTTP 响应结果未知

前端超时、断网不等于服务器生成失败。
先读取同一 discussion 和已有计数，不创建新任务。

若确认请求响应丢失：
先 GET 核对是否已 lineup\_confirmed；
已经确认则继续只读验收；
仍是相同待确认版本时，才按既有幂等契约明确重试确认。

确认重试不得触发模型请求。

若无法读取服务器状态，
报告“结果暂未核实”，不得写成业务失败或自动重新生成。

七、预算与异常处置

每次模型发送前由现有保护持久化计数。
超时、传输中断和结果未知都不退还预算。

区分：

- 保守预算计数；
- 有证据的实际发送次数；
- 成功响应次数。

无法证明请求是否到达供应商时，
标记不确定，不写成“肯定未发送”或“肯定未计费”。

一旦本次 generation 成功或最终失败，
本轮不再允许新的生成调用。

本地取消不能作为“远端一定停止”的证明。
停止后不得用另一请求探测或补偿。

启动前的本地故障可以在不调用模型的情况下修复；
一旦已经开始真实 generation，不得通过修复、重启、
更换标识或修改预算重新获得调用机会。

不发送真实第三次请求来证明预算上限；
这项能力以已有本地测试和当前保护记录为依据。

八、证据与本轮修改范围

优先更新现有 docs/stage-4d-validation.md，
将 4D-A 本地测试和 4D-B 真实结果分开。

记录：

- 实际执行时间、入口与代码版本；
- 非敏感模型配置；
- discussion/generation/阵容版本；
- 每次请求的状态、耗时和安全错误类别；
- 请求模型名、响应模型名（若返回）；
- 实际返回的 usage；
- 总调用次数与最终预算状态；
- 生成、保存、展示、确认、刷新各步骤结果；
- 经过筛选的公开阵容及必要截图。

usage 未返回则写“未取得”。
不估造费用，不将预算计数等同于供应商账单。

不得归档：
真实密钥、Authorization、完整配置、
完整上游原始响应、reasoning\_content、未脱敏异常对象。

截图至少覆盖同一阵容的：

- 待确认；
- 已确认；
- 刷新后的已确认状态。

若生成失败，只保存实际失败界面和安全证据，
不制造成功截图，也不为补图再次生成。

本轮原则上只新增或更新证据、运行记录和必要验证脚本。
若确有代码缺陷，先本地复现、最小修复并运行相关回归；
不要大规模重构，不追加新功能。

四阶段测试数据口径保持清楚：
既有 254 后端、69 前端、26 Fake E2E 是此前结果。
本轮没重跑的标明“沿用 4D-A 记录”，不能写成本轮新通过。
实际重跑的项目另列命令、数量和退出码。

九、收尾、Git 与最终回复

使用现有机制关闭本轮后续真实调用权限，
保留计数、绑定和终态，不清空记录。

关闭本轮启动的服务和浏览器上下文，
核对对应端口，不影响其他进程。
保留用户私有配置，不擅自修改或删除。

按现有格式追加本轮开发 Prompt 和真实过程记录。
不要复制任何包含真实密钥的历史消息进入仓库。

继续使用当前仓库已确认的项目级 Git 身份，
只提交脱敏证据、文档和必要的代码变更。
不提交私有配置、验收数据库或敏感原始输出，
不改写历史、不推送远程。

最终回复：

1. 4D-B：成功 / 失败 / 受阻，以及具体停止位置；
2. 实际使用的受限入口与验收环境；
3. 唯一 discussion/generation 和公开阵容摘要；
4. 各次请求结果、耗时、可得 usage 和总计数；
5. 保存—展示—确认—刷新各步骤结果；
6. 截图及验证报告的实际路径；
7. 本轮实际运行的检查与代码修改；
8. 调用授权关闭、进程退出和 Git 状态；
9. 尚未验证的能力。

只有这一个样本走通，
才写“单样本真实阵容联调通过”。

不得写成所有话题、全部专家人数、完整产品 E2E、
长期可靠性或生产负载已经验证。

完成后停止，不进入讨论调度、SSE、共识或演播厅开发。
