# 阶段6A真实讨论适配器实施计划

> 使用executing-plans在当前会话逐项实施；用户已明确授权，无子代理/worktree或额外审批。

**Goal:** 四种DiscussionProvider通过DeepSeek协议、本地HTTP替身及正式runner/页面验证，官方请求0。
**Architecture:** 保留runner唯一重试层；阵容与讨论共用一次传输，独立提示词和业务校验；默认Fake，显式注入本地transport。
**Tech Stack:** 既有TypeScript、Node fetch/http、SQLite003、Vitest、Playwright Edge，无新增依赖。

## 限定

固定官方URL、deepseek-flash、thinking.disabled、stream=false、json_object，无tools；不读取私有配置/4D库/预算。每次适配器仅一次请求，保留30秒/10分钟/60秒、2/4并发及B(N)=28N+56。

## A. 上下文与四能力（RED→GREEN）

Files: src/providers/discussion-prompt.ts、deepseek-discussion.ts、tests/unit/deepseek-discussion.test.ts。
接口沿用assessIntent/generateUtterance/extractSynthesis/summarize；输入只取DiscussionInput及当前member/purpose/intent/stopReason，输出unknown经既有parse*验证。意愿512、发言768、综合4096、总结1024输出token上限；整个公开上下文96KiB，消息128KiB、正文16KiB。系统规则独立，用户JSON字段逐项构造。

- [x] 先建未实现骨架与四操作测试：`npm test -- tests/unit/deepseek-discussion.test.ts`，确认业务未实现失败。
- [x] 实现提示词及严格校验，拒绝跨场角色/发言、缺字段、非法引用、模型系统字段；完整最新transcript不截断。
- [x] 相同命令通过；加入无人申请、并行上下文、总结末条、空综合与错误分类回归。

## B. 共享传输与配置

Files: src/providers/deepseek-transport.ts、deepseek.ts、discussion-config.ts、.env.backend.example；tests/integration/discussion-adapter-http.test.ts。
传输接口输入固定配置/messages/signal/deadline与受控错误构造器，返回content字符串；只白名单metric。讨论metric增加operation/taskId/attempt及usage缺失标记，不记录messages。

- [x] 真实HTTP stub验证实际POST、参数、虚拟鉴权；这些在四能力实现后加入且首轮通过，作为补充契约回归，不虚构RED。独立配置先用骨架获得业务RED。
- [x] 抽取阵容既有逻辑，保留错误语义，讨论调用同一函数；独立DISCUSSION_PROVIDER，默认Fake、缺配置即失败且无fallback。
- [x] 验证响应头/空行后正文超时、取消、重定向、永久错误与无隐藏重试；运行全部既有DeepSeek阵容测试。

## C. 正式runner接入与预算

Files: scripts/lib/discussion-stub.mjs（及类型声明）、tests/integration/discussion-adapter-runner.test.ts。
stub按每次请求JSON里的operation/input组装当前上下文响应，不提供整场轨迹。正常链路不替换runner/store。

- [x] 验证中途综合及末条总结、网络/修复同预算、401不重试、取消普通任务仍可总结、迟到失效、两场指标隔离。
- [x] 复用现有预算/事务/期限测试回归；不可把旧Fake验证当适配器独立证据。

## D. 页面与本地HTTP流程

Files: scripts/stage6a-backend.mjs、playwright.stage6a.config.ts、e2e-local/discussion-adapter.spec.ts；必要测试标签在web/src/Studio.tsx。
独立.tmp/stage-6a数据库和41871/41872，stub随机loopback端口，显式虚拟凭据。现有页面正常HTTP/SSE，测试门闩只在本地替身，不改生产runner。

- [x] 创建/确认Fake阵容，再真实讨论适配器→本地HTTP→开场/专家/中途综合/总结/刷新；另总结失败场景。
- [x] 页面标记“真实适配器经本地 HTTP 替身验证”，保持默认Fake标签与旧流程。

## E. 最终验证与收尾

- [x] 最后修改后运行全部后端/前端/原37 E2E/新增本地接入E2E、三类型/两构建，记录真实数量/退出码与警告。
- [x] 保存代表RED/GREEN和安全指标，归档P13原文，更新README/contracts/test-plan/运行设计及stage-6a-validation；6B仅方案。
- [x] 检查历史Prompt、旧schema/4D证据、私有配置忽略、敏感内容和Git范围；提交明确文件，关闭本轮stub/浏览器/服务并检查端口。

自查：不扩展runner、SSE或迁移；测试只注入transport/模型服务边界；没有真实权限入口、隐藏重试或额外修复模型。
