# 阶段5B Fake讨论执行器实施计划

> 执行方式：使用 executing-plans 在当前会话逐任务执行；用户明确禁止子代理/worktree，覆盖Skill默认选项。每项遵循 RED→GREEN→REFACTOR，独立验证后提交。

**Goal:** 已确认阵容经Fake内容驱动执行、增量提炼、有限收尾，正式SQLite与HTTP可读取终态。
**Architecture:** 独立DiscussionProvider与单场runner；SqliteDiscussionStore负责短事务/CAS/预算及事件；调用Limiter由阵容和讨论共同注入。保持001/002不可变，003显式迁移，前端只做严格消费兼容。
**Tech Stack:** 现有Node/TypeScript/Express/node:sqlite/Vitest/React/Playwright；不增加依赖。

## 全局约束
- 只操作当前分支；不读密钥、不加载真实讨论Provider、不触碰stage-4d-live。
- 2场运行含收尾；单场/全局请求2/4；每请求30秒、任务60秒、运行10分钟、收尾60秒。
- 每任务最多2尝试；12成功专家发言；最多2额外主持；独立总结域。
- B(N)=2*[2+12*(N+2)+2*(N+1)]=28N+56，N=1..8为84/112/140/168/196/224/252/280，包含总结预留2次。失败替补也扣同一有限总额，不保证跑满12轮。
- 本轮用户确认覆盖5A阶段划分：允许最小前端运行快照兼容，不增加控制按钮或演播厅。
- 真实SQLite临时fixture、明确Fake、可控计时器；SSE全部留5C。

## A. 003迁移与旧库保留
Files: src/db/schema-v3.ts、migrations.ts；tests/fixtures/schema-v2.sql；tests/integration/runtime-migration.test.ts。
Consumes: migrateDatabase(db,target) 及冻结001/002。
Produces: target=3，新增运行列/utterances/findings/finding_evidence/role_public_states/7种事件。
- [x] RED: `migrateDatabase(db,3); expect(versions).toEqual([1,2,3])`；002种子升级比对所有旧列/成员/事件。
- [x] Run: `npm test -- tests/integration/runtime-migration.test.ts`，预期新迁移未实现失败。
- [x] GREEN: 003复制全部002列，扩CHECK；完整性校验失败整批回滚，迁移账本追加checksum。
- [x] Run同一测试及旧migrations/schema/runtime回归；确认002历史测试不再误称当前最新版。
- [x] REFACTOR并审查只提交迁移与夹具。

## B. 讨论类型、运行时校验与Fake
Files: src/domain/discussion.ts、src/providers/discussion.ts、fake-discussion.ts；tests/unit/discussion.test.ts。
Interfaces: parseIntent(raw,snapshot), parseUtterance(raw,snapshot,purpose), parseSynthesis(raw,snapshot), parseSummary(raw)；DiscussionProvider四方法返回Promise<unknown>。
- [x] RED: 合法短句/跨场引用/未知键/双专家证据；Fake输入transcript变化导致实际输出变化。
- [x] Run: `npm test -- tests/unit/discussion.test.ts`，只接受业务RED。
- [x] GREEN: 精确键/码点/句界/归属，系统字段后端生成；Fake四独立方法及可控hook。
- [x] 同文件GREEN、typecheck；整理提交。

## C. 共享调用槽与取消
Files: src/providers/call-limiter.ts、limited-roster.ts；tests/unit/call-limiter.test.ts。
Interface: CallLimiter.run(discussionId,signal,deadline,operation)，operation取得有效调用槽后才执行。
- [x] RED: 同场2/全局4，排队取消不会执行operation，满队列受控失败。
- [x] Run: `npm test -- tests/unit/call-limiter.test.ts`。
- [x] GREEN: 有界队列、每场公平发槽、期限/取消清理、释放后推进；包装RosterGenerator。
- [x] 独立回归并提交，不接真实配置。

## D. 运行存储、开始/停止与原子事件
Files: src/domain/snapshot.ts、discussion-store.ts；src/db/read-runtime.ts、sqlite-discussion.ts、read-discussion.ts；tests/integration/discussion-store.test.ts。
Interfaces: begin(id,input), state(id), reserve(key,summary), append(key,speech), synthesize(key,items), stop(id,reason), finish(key,summary), fail(id,code), recover()。
- [x] RED: 只有当前确认版本开始、重复幂等；事件失败发言回滚；并发最后额度及旧key失效。
- [x] Run: `npm test -- tests/integration/discussion-store.test.ts`。
- [x] GREEN: 同步事务状态CAS；每公开事务version+1，事件按数量连续分配；内部预算不改公开版本；终态保留全部记录。
- [x] 独立SQLite重开/外键/恢复回归并提交。

## E. runner与调度、综合及总结
Files: src/domain/discussion-service.ts；tests/integration/discussion-runner.test.ts。
Interface: DiscussionService(store,provider,limiter).start(id,body)/stop(id,body)/idle()/close()/recover()/assertAvailable(id?)。
- [x] RED: 正常完整轨迹、单专家、多人/无人申请、重复开始、迟到、两场隔离、总结失败及时间预算。
- [x] Run: `npm test -- tests/integration/discussion-runner.test.ts`，按独立行为小批RED/GREEN。
- [x] GREEN: 登记runner先于异步派发；内容版本冻结，等待全批意愿；选中发言校验提交后综合；stop独立总结域。
- [x] 实际可控时钟验证30秒/60秒/10分钟；所有任务close后槽和定时器清理，回归提交。

## F. HTTP与启动恢复
Files: src/http/app.ts、src/server.ts、src/db/ownership.ts；tests/integration/discussion-http.test.ts。
Interfaces: POST /api/discussions/:id/start {requestId,generationId,lineupRevision}；POST /stop {}；GET运行24字段。
- [x] RED: 202/200幂等、400/404/409/429/503与安全诊断，GET无调用。
- [x] Run: `npm test -- tests/integration/discussion-http.test.ts`。
- [x] GREEN: 正式Express链路、共享Limiter组合、普通Fake入口恢复；单进程边界/库占用保护，不宣称跨进程执行协调。
- [x] HTTP真实请求回归；保持live-server和4D授权文件不变；提交。

## G. 最小前端消费者兼容
Files: web/src/api.ts、runtime-snapshot.ts、App.tsx、LineupPanel.tsx；web/tests/runtime-api.test.tsx。
Consumes: 24字段运行DTO，原19/21字段保持。
- [x] RED: 新状态解析、confirmedAt不等于updatedAt、事件版本可不相等；非法运行字段仍拒绝。
- [x] Run: `npm run test:web -- web/tests/runtime-api.test.tsx`。
- [x] GREEN: 严格联合校验与中文标签，运行态禁止旧阵容动作；不加开始/结束/Transcript界面。
- [x] 前端全回归并提交。

## H. 验收与记录
Files: docs/stage-5b-validation.md、evidence/stage-5b/fake-trace.json、既有设计/记录/README；必要验收脚本。
- [x] 输出完整标记Fake轨迹；29条S5映射真实测试或5C/Q计划。
- [x] `npm test`、`npm run test:web`、`npm run typecheck`、`npm run typecheck:web`、`npm run build`、`npm run build:web`、`npm run typecheck:e2e`、`npm run test:e2e`。
- [x] 独立临时库本地HTTP验收，检查关闭任务/进程，记录真实数量/退出码/RED→GREEN。
- [x] 检查Git范围/敏感形态/历史Prompt前缀，逐路径提交；停止，不进入5C。

自查：全部附件要求分配A–H；SSE/真实能力单独计划；无未定义依赖、无额外系统。具体实现由失败测试推动，接口小修同步此计划和契约。

## 实际执行说明

A–H已按真实任务完成，结果与RED/GREEN见../../stage-5b-validation.md。存储append实际接口为(key,roleId,speech)，角色不能由Provider指定。HTTP占用保护独立放db/ownership.ts；前端解析独立runtime-snapshot.ts，controller无需改写。E2E使用playwright.stage5b.config.ts隔离报告/截图，实际等效CLI参数见证据，未安装包或浏览器。补充提交前期限回滚及相关回归；没有进入SSE或真实模型实施。提交与最终文件核对完成情况以Git及验证报告为准。
