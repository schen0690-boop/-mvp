# Stage6B Implementation Plan

> 当前会话逐任务executing-plans；用户已批准，无子代理/worktree。先本地TDD和全回归、提交准备代码，再唯一真实run；不修改运行中的代码或参数。

**Goal:** 预置阵容，2次专家/120秒普通/60秒总结，独立一次授权20次（18普通+2总结），真实单样本后关闭。
**Architecture:** 保留003和B(N)默认技术预算；SqliteDiscussionStore仅接受后端受控的绑定run短参数，已有deadline持久化；独立只增不改授权文件保存短参数/run和发送预约。固定项目路径锚点防止换目录重新初始化。guard只包四能力DeepSeek，不改调度算法。
**Tech Stack:** 已有Node/TS/SQLite/Express/React/SSE/Vitest/Edge，无依赖升级。

## A. 短运行（RED→GREEN）

Files: src/domain/discussion-store.ts、src/db/sqlite-discussion.ts、src/domain/discussion-service.ts、tests/integration/short-discussion.test.ts。
接口：Store构造可选acceptance={discussionId,runId,expertTurns:2,runDurationMs:120000,beforeStart:()=>void}。限定仅绑定讨论；begin先执行一次授权claim，再存runId/deadline；append第2专家原子stopping；默认仍12/600000。runner定时器依据已持久化deadline。额外CallBudgetError仅转既有预算收尾，不扩大其他重试。
- [ ] 先测试3条transcript、1次提炼/1总结、9次调用和120秒期限；骨架RED，再最小实现GREEN。验证正常默认12/10分钟、重复开始/未知讨论不改变授权。

## B. 独立一次授权（RED→GREEN）

Files: src/live/discussion-authorization.ts、src/live/guarded-discussion.ts、tests/unit/discussion-authorization.test.ts、tests/integration/guarded-discussion.test.ts。
manifest={authorizationId,discussionId,runId,固定参数}；独占服务锁，wx+fsync不可变普通slot1..18/总结slot1..2；任务attempt最多2，summary绑定唯一task；started/closed标记不可删改。预约在实际transport前，queued不算发送；guard每次核对当前run/epoch/source/status/deadline，只允许四任务。取消/错误不退还；记录fetch调用及响应安全metric，缺usage未取得。确定性错误不重试，预算耗尽沿既有stop/summary。
- [ ] RED证明上限/绑定/重复task/关闭未实现，再实现与重开/并发争额/未知消耗/取消/修复共享预算测试。
- [ ] 扩展metric仅白名单请求/响应模型、开始时间和validResult；先断言失败后实现，不改变提示词。

## C. 受限入口与预置阵容

Files: src/live/stage6b-settings.ts、src/prepare-stage6b.ts、src/live-stage6b.ts、scripts/stage6b-live-ui.mjs、package.json、web/src/provider-label.ts。
固定项目.local/stage-6b-live独立DB/authorization与.local/stage-6b-once.json锚点；prepare只初始化一次，预置角色经LineupService生成/确认，无模型阵容。live只打开已准备记录；恢复只读/中断，绝不续跑；全程仅bound start/stop POST；其余写操作拒绝。程序化读取现有私有配置且不输出原文，检查只回模式/模型/参数/已配置。
- [ ] 入口保护和本地HTTP短run验证通过；UI标签明确预置阵容/真实讨论。本地与真实浏览器脚本无自动重试，未知结果仅GET同场，不补发start。

## D. 回归与提交

- [ ] 后端全部、前端、旧37 Fake E2E、6A2 HTTP E2E、三类型/两构建；证据写stage-6b，保留历史；源码哈希/敏感检查、提交准备版本。
- [ ] 安全配置/忽略/端口/独立记录检查，若密钥缺失或旧计数阻断真实调用。官方请求仍0直至用户授权的页面start。

## E. 一次真实运行与收尾

- [ ] 打开唯一已确认阵容，点击一次start，观察开场→2专家及1中途提炼→唯一总结；允许实际更早结束，保存白名单快照/指标/SSE与实际截图。
- [ ] 终态或异常关闭授权，记录保守预算、fetch调用、HTTP成功、usage缺失/未知；停自有进程/浏览器，安全只读重开同库比对。
- [ ] P14原文追加、stage-6b-validation/README/设计实际参数及问题记录，脱敏证据提交；不补第2场、不推送。

自检：固定模型/四token上限、0真实阵容、4D无访问；默认schema/预算不改；短参数在开始前持久化manifest，重启不自动继续。所有未实现工作不声明通过。
