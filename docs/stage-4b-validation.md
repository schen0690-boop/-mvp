# 阶段4B实际验证记录

本轮只Fake Provider、阵容后端、非破坏迁移和最小消费者兼容。P6已明确确认4A；实际实施计划位于superpowers/plans/2026-09-16-lineup-backend.md。本文件随真实任务完成追加，不预填结果。

## 任务A：001接管

- 01-migration001-red：7项因迁移未实现失败，退出1。
- 02-migration001-green：同组7项与原79项后端回归合计86通过，退出0；严格类型检查退出0。
- tests/fixtures/schema-v1.sql从2ae575a的真实阶段2DDL冻结，独立于新迁移源码；001不引入阵容字段。未知CHECK、半表、未知触发器拒绝；原数据/事件、重复执行与重开均验证。

命令起止时间、stdout/stderr和源码哈希保存evidence/stage-4b。测试库仅.tmp/stage-4b及既有测试辅助创建的新临时目录，无用户库迁移。

## 任务B：002与显式初始化

03-migration002-red：新增6项行为失败、原7项通过，退出1。04-migration002-green：全后端92项通过（原79项保留，schema表数量断言按002明确更新），退出0；类型检查退出0。实际从冻结旧库升级、保留NUL话题和事件、末尾完整性失败整体回滚、复合FK/唯一/CHECK、重开和重复均通过。启动缺失/旧库拒绝而不自动迁移；显式初始化在临时旧库先保留一致新备份，重复最新初始化不新增备份。没有执行开发库迁移。首次apply_patch因同文件重复操作格式被拒绝，未落地修改；修正补丁格式后执行，不算业务RED。

## C/D 校验与Fake
05初次RED发现参数化数组被展开为多参数，按systematic-debugging修正测试数据包装；06重新运行38项中29项因未实现失败（9项仅要求拒绝，骨架即可拒绝，不计独立RED）。07领域38项通过、Fake13项因NOT_IMPLEMENTED失败；08领域/Fake/旧输入共89项通过，退出0，类型检查退出0。候选结构与业务错误分开，系统字段拒绝、NFKC判重、Unicode长度及调色板验证；Fake的13种内置模式及受控延迟回调使用生产边界。没有调用真实模型。公开snapshot校验随E/H消费者接入。

## E 生成服务与事务
09基本生成2项业务RED；10新增行为通过但5项旧库夹具失败，原因是旧schema建数借用已扩展当前业务层；改为直接冻结旧DDL/INSERT，001验证旧SQL，002之后验证正式业务API。11全145项通过。12失败分类/预算/重新生成/容量7项RED；容量断言失败还导致等待清理超时，后续close增加取消。13修改遗漏闭合括号造成解析失败（不计业务RED），14修正后9项通过。15超时/存储/恢复5项RED；16全158项通过。17补充未来状态锁定纯决策回归后全168项通过，退出0；类型检查退出0。
真实SQLite覆盖成员/事件中途写入故障、失败状态二次失败屏蔽读取、重启中断恢复、跨讨论CAS、A两次尝试60秒终结后B成功且A迟到不改事件/快照。模拟时钟只用于期限，Provider受控屏障而非固定睡眠。只保留最后成功成员集合；替换前失败仍留旧行但公开roles为空，成功旧版本由public_events保留。

## F/G 确认与HTTP
18确认6项中5项因NOT_IMPLEMENTED为RED，19确认/生成/韧性21项GREEN。20真实Express的11项新路由因404等目标行为未实现为RED；21全后端185项通过退出0，类型检查退出0。覆盖202受理/当前重放200、输入400/不存在404/版本状态409/容量429/存储503、异步Provider失败GET200、并发确认单事件、原创建请求重放返回当前快照。生产入口显式Fake、监听前恢复遗留生成，关闭时先取消并收束自有任务再关库。

## H 最小消费者兼容
22新增7项中6项RED（原18项仍通过），23前端25项全部通过，退出0；前端类型检查退出0。created维持19字段，其他四态严格21字段；成员字段/顺序/颜色/双版本/安全notice被校验。仅中文状态文本，没有阵容按钮/卡片/确认UX或轮询。24后端构建、25前端构建、26原阶段3九项Edge E2E均退出0（12.9秒）。Playwright只对新建.tmp/stage-4b/browser-*库显式初始化，生产启动不迁移；报告和布局截图改到stage-4b，后来发现另两处截图路径遗漏，处理见下文。NO_COLOR/FORCE_COLOR提示不影响退出码。

## 最终自查补强
提交内部复核总期限：27新增“开始持久化前已超时”和“插入途中跨过期限”两项均RED（原实现错误返回true）；28补丁定位到相似代码段导致解析失败，不计业务RED；精确修正后29全189项通过退出0，类型检查退出0。成功事务在获取写锁后及事件写完、COMMIT前复核单调期限，过期整笔回滚，再由service记录TIMEOUT。补充A迟到失败和002复制中途遇损坏旧数据的回滚回归。期限为进程单调时钟的提交检查，非操作系统硬实时保证。

## 最终验证汇总

| 实际命令（根目录） | 结果 | 退出码 | evidence/stage-4b证据 |
|---|---|---|---|
| npm run typecheck（调用同一本地tsc配置） | 通过，最终测试文件也受严格检查 | 0 | 30、42、45 |
| npm run typecheck:web | 通过 | 0 | 31 |
| npm run typecheck:e2e | 通过 | 0 | 32、43 |
| npm test | 190项：99单元＋91 SQLite/HTTP集成 | 0 | 41；随后仅迁移历史断言补充，44同组12项通过 |
| npm run test:web | 25项，含原18项 | 0 | 34 |
| npm run build | 正式后端编译通过 | 0 | 35 |
| npm run build:web | 正式前端构建通过，22模块 | 0 | 36 |
| npm run test:e2e | 10项：原9＋状态消费者兼容1 | 0 | 37、40；最终40为14.6秒 |
| node scripts/http-smoke.mjs | 两次初始化/启动，草稿创建查询与重开一致，监听关闭 | 0 | 38 |
| node scripts/http-smoke.mjs --lineup | Fake生成202、确认200、重开保持确认快照、版本001/002、监听关闭 | 0 | 39 |

记录器直接调用项目内tsc/vitest/vite/playwright入口，等价于package.json中的命令，不使用探针依赖。41补充“两场真实生成交错完成”和恢复保留ready；44在原迁移用例中补充编号缺号拒绝，不增加计数。HTTP29项包含在91中，迁移12项和维护入口2项同样包含，不重复相加。

收尾检查发现e2e/drafts及interaction仍有两处stage-3截图路径，本轮曾覆盖工作区旧图；已把新图另存stage-4b，从已有Git精确恢复原图并修改路径。40重跑全10项通过，阶段3证据对基线无差异。浏览器输出只有已知颜色环境变量提示；无依赖/浏览器安装。已目视查看普通桌面及窄屏详情截图，范围仅草稿布局回归，不宣称完整阵容卡片UI已验证。

一次批量文档Shell命令被自动审批拒绝（工具仅给blocked by policy），未执行；随后用直接文件补丁完成授权文档更新，无权限放宽或全局修改。这不计业务RED。所有业务RED和准备错误均原样保留于JSON，未倒填“通过”。

### 实际交付与限制

- 当前迁移版本002；001严格原schema、002才阵容字段。旧字段/事件、幂等创建、外键/唯一约束、整事务回滚与重开验证通过，仅测试自建旧库，未升级用户运行库。
- 公开成员8字段；数据库13列含内部关联、generation_version（该成员组的lineupRevision）、name_key、时间。一个主持人＋N专家由业务整组校验；数据库唯一/FK/CHECK补充保障。
- 当前generation ID/number、成功revision与双版本确认已实现；生成中不可替换；A晚于B成功/失败均不会写入B。重新生成失败保留旧行但roles=[]且禁止旧确认。
- 仅Fake，未来外部服务取消/费用/能力、真实语义多样性与隐藏内容质量未验证；没有真实API/密钥读取、付费调用、SSE、调度、共识或完整阵容UI。
- 默认本地有效任务容量4、最多两次调用、30/60秒为已确认设计参数；不是跨进程协调或硬实时保证。node:sqlite/Windows10已有基线风险、Edge本机成功不代表官方系统支持；未开展npm漏洞审计。
- P6真实全文已归档，旧Prompt前缀及历史阶段证据/依赖保持。最终源文件哈希、Git范围和限定敏感形态检查见46-final-review。未把模式通过当作真实模型验证。

### 本轮真实Git记录

e9259a6（001）→59ca871（002）→f836882（领域/Fake）→7bc88f8（生成）→8efd9ec（确认/HTTP）→8ed9fb3（前端兼容）→c605b3a（提交内期限修正）。随后收尾提交包括用例映射、最终验证、来源检查和README；以git log真实当前时间为准，无空提交、rebase、squash、倒填或远程推送。

下一阶段仅建议4C阵容按钮/卡片/确认/失败重试与冲突刷新交互，仍可用Fake完成完整阵容局部E2E；真实模型接入需单独授权。本轮到4B停止。

最终本机检查：41841/41842均无监听；两组冒烟各自子进程正常退出0且监听关闭。暂定身份仍为仓库级schen / cs064210@163.com。源码与最后构建/测试记录哈希一致，32个S4编号和相对文档链接检查通过；历史阶段2/3证据、探针、依赖与原Prompt保持。敏感扫描只覆盖本轮变更的高置信度模式，无命中，不等同安全审计。
