# 阶段4B实际验证记录

本轮只Fake Provider、阵容后端、非破坏迁移和最小消费者兼容。P6已明确确认4A；实际实施计划位于superpowers/plans/2026-09-16-lineup-backend.md。本文件随真实任务完成追加，不预填结果。

## 任务A：001接管

- 01-migration001-red：7项因迁移未实现失败，退出1。
- 02-migration001-green：同组7项与原79项后端回归合计86通过，退出0；严格类型检查退出0。
- tests/fixtures/schema-v1.sql从2ae575a的真实阶段2DDL冻结，独立于新迁移源码；001不引入阵容字段。未知CHECK、半表、未知触发器拒绝；原数据/事件、重复执行与重开均验证。

命令起止时间、stdout/stderr和源码哈希保存evidence/stage-4b。测试库仅.tmp/stage-4b及既有测试辅助创建的新临时目录，无用户库迁移。

## 任务B：002与显式初始化

03-migration002-red：新增6项行为失败、原7项通过，退出1。04-migration002-green：全后端92项通过（原79项保留，schema表数量断言按002明确更新），退出0；类型检查退出0。实际从冻结旧库升级、保留NUL话题和事件、末尾完整性失败整体回滚、复合FK/唯一/CHECK、重开和重复均通过。启动缺失/旧库拒绝而不自动迁移；显式初始化在临时旧库先保留一致新备份，重复最新初始化不新增备份。没有执行开发库迁移。首次apply_patch因同文件重复操作格式被拒绝，未落地修改；修正补丁格式后执行，不算业务RED。

## C/D 校验与Fake
05初次RED发现参数化数组被展开为多参数，按systematic-debugging修正测试数据包装；06重新运行38项中29项因未实现失败（9项仅要求拒绝，骨架即可拒绝，不计独立RED）。07领域38项通过、Fake13项因NOT_IMPLEMENTED失败；08领域/Fake/旧输入共89项通过，退出0，类型检查退出0。候选结构与业务错误分开，系统字段拒绝、NFKC判重、Unicode长度及调色板验证；Fake正常和13种模式使用生产校验。没有调用真实模型。公开snapshot校验随E/H消费者接入。

## E 生成服务与事务
09基本生成2项业务RED；10新增行为通过但5项旧库夹具失败，原因是旧schema建数借用已扩展当前业务层；改为直接冻结旧DDL/INSERT，001验证旧SQL，002之后验证正式业务API。11全145项通过。12失败分类/预算/重新生成/容量7项RED；容量断言失败还导致等待清理超时，后续close增加取消。13修改遗漏闭合括号造成解析失败（不计业务RED），14修正后9项通过。15超时/存储/恢复5项RED；16全158项通过。17补充未来状态锁定纯决策回归后全168项通过，退出0；类型检查退出0。
真实SQLite覆盖成员/事件中途写入故障、失败状态二次失败屏蔽读取、重启中断恢复、跨讨论CAS、A两次尝试60秒终结后B成功且A迟到不改事件/快照。模拟时钟只用于期限，Provider受控屏障而非固定睡眠。只保留最后成功成员集合；替换前失败仍留旧行但公开roles为空，成功旧版本由public_events保留。

## F/G 确认与HTTP
18确认6项中5项因NOT_IMPLEMENTED为RED，19确认/生成/韧性21项GREEN。20真实Express的11项新路由因404等目标行为未实现为RED；21全后端185项通过退出0，类型检查退出0。覆盖202受理/当前重放200、输入400/不存在404/版本状态409/容量429/存储503、异步Provider失败GET200、并发确认单事件、原创建请求重放返回当前快照。生产入口显式Fake、监听前恢复遗留生成，关闭时先取消并收束自有任务再关库。
